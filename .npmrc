# Fail fast on a Node version outside package.json engines, instead of installing anyway.
engine-strict=true
# Pin exact versions on `pnpm add`, matching the exact-version policy in AGENTS.md §5.7.
save-exact=true
