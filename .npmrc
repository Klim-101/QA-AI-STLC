# Fail fast on a Node version outside package.json engines, instead of installing anyway.
engine-strict=true
# Pin exact versions on `npm install <pkg>`, matching the exact-version policy in AGENTS.md §5.7.
save-exact=true
